# Pin the lint ruleset explicitly so `ruff check` is deterministic across ruff
# versions (defaults have drifted between releases). This mirrors the ruleset
# the codebase has always been clean under: E4/E7/E9 + Pyflakes.
line-length = 120

[lint]
select = ["E4", "E7", "E9", "F"]
